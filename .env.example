# Optional — only needed if you regenerate blurbs with Muse Spark
# (`npm run blurbs`). Country blurbs ship curated in data/country_blurbs.json.
#
# Meta Model API — Muse Spark 1.1
# Docs: https://dev.meta.ai/docs/getting-started/overview/
# Base URL: https://api.meta.ai/v1
# Model:    muse-spark-1.1
#
# Do not prefix with VITE_ — secrets stay script/server-side only.
MODEL_API_KEY=
